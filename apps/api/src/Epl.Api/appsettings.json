{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  },

  "//ConnectionStrings": "Override via env var ConnectionStrings__Sql (prod) or dotnet user-secrets (dev). docker-compose injects this from the env file.",
  "ConnectionStrings": {
    "Sql": ""
  },

  "//Bootstrap": "Override via env vars Bootstrap__AdminEmail / Bootstrap__AdminPassword. The seeder logs a warning and skips creating the admin if either is empty.",
  "Bootstrap": {
    "AdminEmail":    "",
    "AdminPassword": "",
    "AdminFullName": "EPL Admin"
  },

  "Gallery": {
    "Images": [
      { "Url": "https://eplmedia.blob.core.windows.net/gallery/hero-banner.jpg", "Alt": "EPL Season 2", "Category": "Highlights" }
    ]
  }
}
