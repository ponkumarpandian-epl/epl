{
  "name": "@epl/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end Playwright tests for the EPL web app (T-2 entries + T-3 bracket flows).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.9.0"
  }
}
